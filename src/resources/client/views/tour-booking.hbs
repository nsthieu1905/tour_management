<div class="bg-gray-50 min-h-screen py-8">
  <div class="max-w-6xl mx-auto px-4">
    <!-- Header -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Đặt tour</h1>
      <p class="text-gray-600">{{ tour.name }}</p>
    </div>

    <!-- Step Indicators -->
    <div class="flex items-center justify-between mb-12">
      <!-- Step 1 -->
      <div class="flex flex-col items-center flex-1">
        <div
          id="step-1-circle"
          class="w-12 h-12 bg-blue-500 text-white rounded-full flex items-center justify-center font-bold text-lg mb-2 cursor-pointer hover:bg-blue-600 transition"
        >
          <span id="step-1-icon">1</span>
        </div>
        <p class="text-sm font-medium text-gray-700">Thông tin</p>
      </div>

      <!-- Line 1 -->
      <div id="line-1" class="flex-1 h-1 bg-gray-300 mx-2 mb-8"></div>

      <!-- Step 2 -->
      <div class="flex flex-col items-center flex-1">
        <div
          id="step-2-circle"
          class="w-12 h-12 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center font-bold text-lg mb-2 cursor-pointer hover:bg-gray-400 transition"
        >
          <span id="step-2-icon">2</span>
        </div>
        <p class="text-sm font-medium text-gray-700">Thanh toán</p>
      </div>
    </div>

    <!-- Content -->
    <div
      class="bg-white rounded-lg shadow-md p-8"
      data-tour-id="{{ tour._id }}"
      data-tour-price="{{ tour.price }}"
      data-tour-capacity="{{ tour.capacity.max }}"
      data-tour-available="{{ subtract tour.capacity.max tour.capacity.current }}"
    >
      <!-- Step 1: Thông tin -->
      <div id="step-1-content" class="space-y-6">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">
          Thông tin người đặt tour
        </h2>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <div class="space-y-6">
            <!-- User Info Section -->
            <div class="bg-white border border-gray-200 rounded-lg p-6 mb-6">
              <h3 class="font-semibold text-gray-900 mb-4">
                Thông tin của bạn
              </h3>
              <div class="space-y-4">
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2"
                    >Họ và tên <span class="text-red-500">*</span></label
                  >
                  <input
                    type="text"
                    id="customer-name"
                    placeholder="Nhập tên đầy đủ"
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required
                  />
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2"
                    >Email <span class="text-red-500">*</span></label
                  >
                  <input
                    type="email"
                    id="customer-email"
                    placeholder="Nhập email"
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required
                  />
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2"
                    >Điện thoại <span class="text-red-500">*</span></label
                  >
                  <input
                    type="tel"
                    id="customer-phone"
                    placeholder="Nhập số điện thoại"
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required
                  />
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-3"
                    >Chọn ngày khởi hành
                    <span class="text-red-500">*</span></label
                  >
                  <select
                    id="departure-date"
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                  >
                    <option value="">-- Chọn ngày --</option>
                    {{#each tour.departureDates}}
                    <option value="{{ this._id }}" data-date="{{ this.date }}">
                      {{ formatDate this.date }} - {{ formatPrice this.price
                      }}₫/người
                    </option>
                    {{/each}}
                  </select>
                </div>
              </div>
            </div>

            <div class="bg-white border border-gray-200 rounded-lg p-6">
              <h3 class="font-semibold text-gray-900 mb-4">Hành khách</h3>
              <div class="text-sm text-gray-600 mb-4">
                Còn lại
                <span id="seats-remaining" class="font-semibold">0</span>
                chỗ
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div class="border border-gray-200 rounded-lg p-4">
                  <div class="flex items-center justify-between">
                    <div>
                      <div class="font-semibold text-gray-900">Người lớn</div>
                      <div class="text-sm text-gray-600">Từ 12 trở lên</div>
                    </div>
                    <div class="flex items-center gap-3">
                      <button
                        type="button"
                        class="w-10 h-10 bg-gray-200 text-gray-700 rounded-full flex items-center justify-center hover:bg-gray-300 font-bold text-lg"
                        id="adult-decrease"
                      >
                        −
                      </button>
                      <span
                        id="adult-count"
                        class="w-8 text-center font-semibold text-gray-900"
                        >1</span
                      >
                      <button
                        type="button"
                        class="w-10 h-10 bg-blue-600 text-white rounded-full flex items-center justify-center hover:bg-blue-700 font-bold text-lg"
                        id="adult-increase"
                      >
                        +
                      </button>
                    </div>
                  </div>
                </div>

                <div class="border border-gray-200 rounded-lg p-4">
                  <div class="flex items-center justify-between">
                    <div>
                      <div class="font-semibold text-gray-900">Trẻ em</div>
                      <div class="text-sm text-gray-600">Từ 2 - 11 tuổi</div>
                    </div>
                    <div class="flex items-center gap-3">
                      <button
                        type="button"
                        class="w-10 h-10 bg-gray-200 text-gray-700 rounded-full flex items-center justify-center hover:bg-gray-300 font-bold text-lg"
                        id="child-decrease"
                      >
                        −
                      </button>
                      <span
                        id="child-count"
                        class="w-8 text-center font-semibold text-gray-900"
                        >0</span
                      >
                      <button
                        type="button"
                        class="w-10 h-10 bg-blue-600 text-white rounded-full flex items-center justify-center hover:bg-blue-700 font-bold text-lg"
                        id="child-increase"
                      >
                        +
                      </button>
                    </div>
                  </div>
                </div>

                <div
                  class="border border-gray-200 rounded-lg p-4 md:col-span-2"
                >
                  <div class="flex items-center justify-between">
                    <div>
                      <div class="font-semibold text-gray-900">Em bé</div>
                      <div class="text-sm text-gray-600">Dưới 2 tuổi</div>
                    </div>
                    <div class="flex items-center gap-3">
                      <button
                        type="button"
                        class="w-10 h-10 bg-gray-200 text-gray-700 rounded-full flex items-center justify-center hover:bg-gray-300 font-bold text-lg"
                        id="infant-decrease"
                      >
                        −
                      </button>
                      <span
                        id="infant-count"
                        class="w-8 text-center font-semibold text-gray-900"
                        >0</span
                      >
                      <button
                        type="button"
                        class="w-10 h-10 bg-blue-600 text-white rounded-full flex items-center justify-center hover:bg-blue-700 font-bold text-lg"
                        id="infant-increase"
                      >
                        +
                      </button>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="space-y-4">
            {{#if tour.partnerServices}}
            <div class="bg-white border border-gray-200 rounded-lg p-6">
              <h3 class="font-semibold text-gray-900 mb-4">Dịch vụ</h3>

              <div class="space-y-4">
                <div>
                  <h4 class="text-sm font-semibold text-gray-900 mb-2">
                    Dịch vụ đã bao gồm
                  </h4>
                  <div class="space-y-2">
                    {{#each tour.partnerServices}} {{#if
                    this.includedInTourPrice}}
                    <div
                      class="p-3 bg-gray-50 border border-gray-200 rounded-lg included-service-item"
                      data-service-name="{{this.serviceId.name}}"
                      data-partner-name="{{this.serviceId.partnerId.name}}"
                    >
                      <div class="flex justify-between gap-3">
                        <div>
                          <div class="font-medium text-gray-900">
                            {{this.serviceId.partnerId.name}} -
                            {{this.serviceId.name}}
                          </div>
                          {{#if this.note}}
                          <div class="text-xs text-gray-600 mt-1">
                            {{this.note}}
                          </div>
                          {{/if}}
                        </div>
                      </div>
                    </div>
                    {{/if}} {{/each}}
                  </div>
                </div>

                <div>
                  <h4 class="text-sm font-semibold text-gray-900 mb-2">
                    Dịch vụ chưa bao gồm
                  </h4>
                  <div id="extras-list" class="space-y-2">
                    {{#each tour.partnerServices}} {{#unless
                    this.includedInTourPrice}}
                    <div class="p-3 bg-white border border-gray-200 rounded-lg">
                      <label class="flex items-center gap-3 cursor-pointer">
                        <input
                          type="checkbox"
                          class="w-4 h-4 text-blue-600 extra-service-checkbox"
                          data-service-id="{{this.serviceId._id}}"
                          data-price="{{this.serviceId.price}}"
                          data-max-qty="{{this.quantity}}"
                          data-service-name="{{this.serviceId.name}}"
                          data-partner-name="{{this.serviceId.partnerId.name}}"
                        />
                        <div class="flex-1">
                          <div class="font-medium text-gray-900">
                            {{this.serviceId.partnerId.name}} -
                            {{this.serviceId.name}}
                          </div>
                          {{#if this.note}}
                          <div class="text-xs text-gray-600 mt-1">
                            {{this.note}}
                          </div>
                          {{/if}}
                        </div>
                        <div class="text-sm font-semibold text-gray-900">
                          {{formatPrice this.serviceId.price}}₫
                        </div>
                      </label>

                      <div
                        class="mt-3 hidden extra-service-qty"
                        data-for="{{this.serviceId._id}}"
                      >
                        <label class="block text-sm text-gray-700 mb-2"
                          >Số lượng</label
                        >
                        <div class="flex items-center gap-3">
                          <button
                            type="button"
                            class="w-9 h-9 bg-gray-200 text-gray-700 rounded-full flex items-center justify-center hover:bg-gray-300 font-bold text-lg extra-service-qty-decrease"
                            data-service-id="{{this.serviceId._id}}"
                          >
                            −
                          </button>
                          <input
                            type="number"
                            min="1"
                            max="{{this.quantity}}"
                            value="1"
                            readonly
                            class="w-24 text-center border border-gray-300 rounded-lg px-3 py-2 text-lg font-medium bg-gray-50 [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none extra-service-qty-input"
                            data-service-id="{{this.serviceId._id}}"
                            data-price="{{this.serviceId.price}}"
                          />
                          <button
                            type="button"
                            class="w-9 h-9 bg-blue-600 text-white rounded-full flex items-center justify-center hover:bg-blue-700 font-bold text-lg extra-service-qty-increase"
                            data-service-id="{{this.serviceId._id}}"
                          >
                            +
                          </button>
                        </div>
                      </div>
                    </div>
                    {{/unless}} {{/each}}
                  </div>
                  <div id="extras-empty" class="text-sm text-gray-500 hidden">
                    Chưa có dịch vụ chưa bao gồm
                  </div>
                </div>
              </div>
            </div>
            {{/if}}

            <!-- Coupon Section -->
            <div class="bg-white border border-gray-200 rounded-lg p-6">
              <label class="block text-sm font-medium text-gray-700 mb-3"
                >Mã giảm giá (nếu có)</label
              >
              <div class="flex space-x-2">
                <input
                  type="text"
                  id="coupon-code"
                  placeholder="Nhập mã giảm giá"
                  class="flex-1 px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 uppercase"
                />
                <button
                  type="button"
                  id="apply-coupon-btn"
                  class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 disabled:bg-gray-400 disabled:cursor-not-allowed"
                >
                  Áp dụng
                </button>
              </div>
              <div id="coupon-message" class="text-sm mt-2 hidden"></div>
            </div>

            <!-- Tour Price Info -->
            <div
              class="bg-gray-50 border border-gray-200 rounded-lg p-6 space-y-2"
            >
              <div class="flex justify-between">
                <span class="text-gray-600">Giá một người:</span>
                <span class="font-medium text-gray-900" id="unit-price"
                  >{{ formatPrice tour.price }}₫</span
                >
              </div>
              <div class="flex justify-between">
                <span class="text-gray-600">Số lượng:</span>
                <span class="font-medium text-gray-900"
                  ><span id="total-guests">1</span> người</span
                >
              </div>
              <div class="flex justify-between">
                <span class="text-gray-600">Dịch vụ:</span>
                <span class="font-medium text-gray-900" id="extras-total"
                  >0₫</span
                >
              </div>
              <div
                id="discount-summary-row"
                class="hidden flex justify-between"
              >
                <span class="text-gray-600">Giảm giá:</span>
                <span
                  class="font-medium text-green-600"
                  id="discount-summary-amount"
                  >0₫</span
                >
              </div>
              <div id="services-breakdown" class="text-sm text-gray-700"></div>
              <div
                class="border-t border-gray-300 pt-2 mt-2 flex justify-between"
              >
                <span class="font-semibold text-gray-900">Tổng tiền:</span>
                <span class="font-bold text-lg text-blue-600" id="subtotal"
                  >{{ formatPrice tour.price }}₫</span
                >
              </div>
            </div>

            <!-- Next Button -->
            <button
              type="button"
              id="next-step-1"
              class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 font-semibold text-lg"
            >
              Tiếp tục đến thanh toán
            </button>
          </div>
        </div>
      </div>

      <!-- Step 2: Thanh toán -->
      <div id="step-2-content" class="space-y-6 hidden">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">Thanh toán</h2>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <div class="space-y-6">
            <!-- Order Summary -->
            <div
              class="bg-gray-50 border border-gray-200 rounded-lg p-6 space-y-4"
            >
              <h3 class="font-semibold text-gray-900 text-lg">
                Tóm tắt đơn hàng
              </h3>

              <div class="space-y-2 text-sm border-b border-gray-300 pb-4">
                <div class="flex justify-between">
                  <span class="text-gray-600">Tour:</span>
                  <span class="font-medium text-gray-900">{{ tour.name }}</span>
                </div>
                <div class="flex justify-between">
                  <span class="text-gray-600">Số khách:</span>
                  <span class="font-medium text-gray-900" id="summary-guests"
                    >1 người</span
                  >
                </div>
                <div class="flex justify-between">
                  <span class="text-gray-600">Ngày khởi hành:</span>
                  <span class="font-medium text-gray-900" id="summary-date"
                    >--</span
                  >
                </div>
                <div class="flex justify-between">
                  <span class="text-gray-600">Giá cơ bản:</span>
                  <span class="font-medium text-gray-900" id="summary-subtotal"
                    >{{ formatPrice tour.price }}₫</span
                  >
                </div>
                <div
                  id="summary-extras-row"
                  class="hidden flex justify-between"
                >
                  <span class="text-gray-600">Dịch vụ:</span>
                  <span class="font-medium text-gray-900" id="summary-extras"
                    >0₫</span
                  >
                </div>
              </div>

              <div
                id="summary-discount"
                class="hidden space-y-2 text-sm border-b border-gray-300 pb-4"
              >
                <div class="flex justify-between">
                  <span class="text-gray-600">Mã giảm giá:</span>
                  <span
                    class="font-medium text-gray-900"
                    id="summary-coupon-code"
                    >--</span
                  >
                </div>
                <div class="flex justify-between text-green-600">
                  <span>Tiền giảm:</span>
                  <span class="font-semibold" id="summary-discount-amount"
                    >0₫</span
                  >
                </div>
              </div>

              <div
                class="flex justify-between text-lg font-bold bg-white border border-gray-300 rounded-lg p-4"
              >
                <span>Tổng thanh toán:</span>
                <span class="text-blue-600" id="summary-total"
                  >{{ formatPrice tour.price }}₫</span
                >
              </div>
            </div>
          </div>

          <div class="space-y-6">
            <!-- Payment Method -->
            <div class="border border-gray-300 rounded-lg p-6">
              <h3 class="font-semibold text-gray-900 mb-4">
                Phương thức thanh toán
              </h3>
              <div class="space-y-3">
                <label
                  class="flex items-center p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"
                >
                  <input
                    type="radio"
                    name="payment-method"
                    value="momo"
                    class="w-4 h-4 text-blue-600 payment-method-radio"
                    checked
                  />
                  <span class="ml-3 font-medium text-gray-900">Ví Momo</span>
                  <span class="ml-auto text-sm text-gray-500"
                    >Sẽ được hướng dẫn chuyển khoản</span
                  >
                </label>
                <label
                  class="flex items-center p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"
                >
                  <input
                    type="radio"
                    name="payment-method"
                    value="bank_transfer"
                    class="w-4 h-4 text-blue-600 payment-method-radio"
                  />
                  <span class="ml-3 font-medium text-gray-900"
                    >Chuyển khoản ngân hàng</span
                  >
                </label>
                <label
                  class="flex items-center p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"
                >
                  <input
                    type="radio"
                    name="payment-method"
                    value="cash"
                    class="w-4 h-4 text-blue-600 payment-method-radio"
                  />
                  <span class="ml-3 font-medium text-gray-900"
                    >Thanh toán tại quầy</span
                  >
                </label>
              </div>

              <!-- Bank Transfer QR Code Section (Hidden by default) -->
              <div
                id="bank-transfer-section"
                class="hidden mt-6 p-6 border border-green-200 bg-green-50 rounded-lg"
              >
                <h4 class="font-semibold text-gray-900 mb-4">
                  Chuyển khoản ngân hàng
                </h4>

                <!-- QR Code -->
                <div class="text-center mb-4">
                  <img
                    id="qr-code-img"
                    src=""
                    alt="QR Code chuyển khoản"
                    class="w-48 h-48 mx-auto"
                  />
                </div>

                <!-- Bank Info -->
                <div class="bg-white p-4 rounded-lg space-y-3 mb-4">
                  <div class="flex justify-between items-center border-b pb-2">
                    <span class="text-gray-600">Ngân hàng:</span>
                    <span
                      id="bank-name"
                      class="font-medium text-gray-900"
                    ></span>
                  </div>
                  <div class="flex justify-between items-center border-b pb-2">
                    <span class="text-gray-600">Số tài khoản:</span>
                    <span
                      id="account-number"
                      class="font-medium text-gray-900 font-mono"
                    ></span>
                  </div>
                  <div class="flex justify-between items-center border-b pb-2">
                    <span class="text-gray-600">Tên tài khoản:</span>
                    <span
                      id="account-name"
                      class="font-medium text-gray-900"
                    ></span>
                  </div>
                  <div class="flex justify-between items-center">
                    <span class="text-gray-600">Số tiền:</span>
                    <span
                      id="transfer-amount"
                      class="font-bold text-green-600 text-lg"
                      >0₫</span
                    >
                  </div>
                </div>

                <!-- Transfer Confirmation -->
                <div class="bg-white p-4 rounded-lg">
                  <label
                    class="flex items-start p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"
                  >
                    <input
                      type="checkbox"
                      id="bank-transfer-confirm"
                      class="w-4 h-4 text-green-600 mt-1"
                    />
                    <span class="ml-3 text-sm text-gray-700">
                      Tôi đã chuyển khoản với số tiền chính xác như trên. Nhấn
                      nút xác nhận để hoàn tất đơn hàng.
                    </span>
                  </label>
                </div>

                <p class="text-xs text-gray-500 mt-3 text-center">
                  Vui lòng chuyển khoản trong vòng 30 phút. Hệ thống sẽ tự động
                  xác nhận khi tiền về tài khoản.
                </p>
              </div>
            </div>

            <!-- Payment Note -->
            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
              <p class="text-sm text-blue-900">
                <strong>Lưu ý:</strong> Hệ thống thanh toán đang ở chế độ demo.
                Nhấn "Xác nhận thanh toán" để hoàn tất đơn hàng.
              </p>
            </div>

            <!-- Action Buttons -->
            <div class="flex space-x-4">
              <button
                type="button"
                id="back-to-step-1"
                class="flex-1 bg-gray-300 text-gray-800 py-3 rounded-lg hover:bg-gray-400 font-semibold"
              >
                ← Quay lại
              </button>
              <button
                type="button"
                id="confirm-payment"
                class="flex-1 bg-green-600 text-white py-3 rounded-lg hover:bg-green-700 font-semibold text-lg"
              >
                Xác nhận thanh toán
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script type="module" src="/client/js/booking.js"></script>
<script id="__TOUR_DATA__" type="application/json">
  {{json tour}}
</script>
