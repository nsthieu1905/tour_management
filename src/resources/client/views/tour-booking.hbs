<div class="bg-gray-50 min-h-screen py-8">
  <div class="max-w-2xl mx-auto px-4">
    <!-- Header -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Đặt tour</h1>
      <p class="text-gray-600">{{ tour.name }}</p>
    </div>

    <!-- Step Indicators -->
    <div class="flex items-center justify-between mb-12">
      <!-- Step 1 -->
      <div class="flex flex-col items-center flex-1">
        <div
          id="step-1-circle"
          class="w-12 h-12 bg-blue-500 text-white rounded-full flex items-center justify-center font-bold text-lg mb-2 cursor-pointer hover:bg-blue-600 transition"
        >
          <span id="step-1-icon">1</span>
        </div>
        <p class="text-sm font-medium text-gray-700">Thông tin</p>
      </div>

      <!-- Line 1 -->
      <div id="line-1" class="flex-1 h-1 bg-gray-300 mx-2 mb-8"></div>

      <!-- Step 2 -->
      <div class="flex flex-col items-center flex-1">
        <div
          id="step-2-circle"
          class="w-12 h-12 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center font-bold text-lg mb-2 cursor-pointer hover:bg-gray-400 transition"
        >
          <span id="step-2-icon">2</span>
        </div>
        <p class="text-sm font-medium text-gray-700">Thanh toán</p>
      </div>

      <!-- Line 2 -->
      <div id="line-2" class="flex-1 h-1 bg-gray-300 mx-2 mb-8"></div>

      <!-- Step 3 -->
      <div class="flex flex-col items-center flex-1">
        <div
          id="step-3-circle"
          class="w-12 h-12 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center font-bold text-lg mb-2 cursor-pointer hover:bg-gray-400 transition"
        >
          <span id="step-3-icon">✓</span>
        </div>
        <p class="text-sm font-medium text-gray-700">Thành công</p>
      </div>
    </div>

    <!-- Content -->
    <div
      class="bg-white rounded-lg shadow-md p-8"
      data-tour-id="{{ tour._id }}"
      data-tour-price="{{ tour.price }}"
      data-tour-capacity="{{ tour.capacity.max }}"
    >
      <!-- Step 1: Thông tin -->
      <div id="step-1-content" class="space-y-6">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">
          Thông tin người đặt tour
        </h2>

        <!-- User Info Section -->
        <div class="bg-white border border-gray-200 rounded-lg p-6 mb-6">
          <h3 class="font-semibold text-gray-900 mb-4">Thông tin của bạn</h3>
          <div class="space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2"
                >Họ và tên <span class="text-red-500">*</span></label
              >
              <input
                type="text"
                id="customer-name"
                placeholder="Nhập tên đầy đủ"
                class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                required
              />
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2"
                >Email <span class="text-red-500">*</span></label
              >
              <input
                type="email"
                id="customer-email"
                placeholder="Nhập email"
                class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                required
              />
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2"
                >Điện thoại <span class="text-red-500">*</span></label
              >
              <input
                type="tel"
                id="customer-phone"
                placeholder="Nhập số điện thoại"
                class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                required
              />
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-3"
                >Chọn ngày khởi hành <span class="text-red-500">*</span></label
              >
              <select
                id="departure-date"
                class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
              >
                <option value="">-- Chọn ngày --</option>
                {{#each tour.departureDates}}
                <option
                  value="{{ this._id }}"
                  data-date="{{ formatDate this.date }}"
                >
                  {{ formatDate this.date }} - {{ formatPrice this.price
                  }}₫/người
                </option>
                {{/each}}
              </select>
            </div>
          </div>
        </div>

        <!-- Guest Count Section -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-3"
            >Số lượng khách</label
          >
          <div class="flex items-center space-x-3">
            <button
              type="button"
              id="decrease-guest"
              class="w-10 h-10 bg-gray-200 text-gray-700 rounded-full flex items-center justify-center hover:bg-gray-300 font-bold text-lg"
            >
              −
            </button>
            <input
              type="number"
              id="guest-count"
              value="1"
              min="1"
              max="{{ subtract tour.capacity.max tour.capacity.current }}"
              class="w-24 text-center border border-gray-300 rounded-lg px-3 py-2 text-lg font-medium [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none"
            />
            <button
              type="button"
              id="increase-guest"
              class="w-10 h-10 bg-blue-600 text-white rounded-full flex items-center justify-center hover:bg-blue-700 font-bold text-lg"
            >
              +
            </button>
            <span class="text-sm text-gray-500"
              >Tối đa {{ subtract tour.capacity.max tour.capacity.current }}
              khách</span
            >
          </div>
        </div>

        <!-- Tour Price Info -->
        <div class="bg-gray-50 border border-gray-200 rounded-lg p-4 space-y-2">
          <div class="flex justify-between">
            <span class="text-gray-600">Giá một người:</span>
            <span class="font-medium text-gray-900" id="unit-price"
              >{{ formatPrice tour.price }}₫</span
            >
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">Số lượng:</span>
            <span class="font-medium text-gray-900"
              ><span id="total-guests">1</span> người</span
            >
          </div>
          <div class="border-t border-gray-300 pt-2 mt-2 flex justify-between">
            <span class="font-semibold text-gray-900">Tổng tiền:</span>
            <span class="font-bold text-lg text-blue-600" id="subtotal"
              >{{ formatPrice tour.price }}₫</span
            >
          </div>
        </div>

        <!-- Coupon Section -->
        <div class="border border-gray-300 rounded-lg p-4">
          <label class="block text-sm font-medium text-gray-700 mb-3"
            >Mã giảm giá (nếu có)</label
          >
          <div class="flex space-x-2">
            <input
              type="text"
              id="coupon-code"
              placeholder="Nhập mã giảm giá"
              class="flex-1 px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 uppercase"
            />
            <button
              type="button"
              id="apply-coupon-btn"
              class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 disabled:bg-gray-400 disabled:cursor-not-allowed"
            >
              Áp dụng
            </button>
          </div>
          <div id="coupon-message" class="text-sm mt-2 hidden"></div>
          <div
            id="discount-info"
            class="mt-3 p-3 bg-green-50 border border-green-200 rounded-lg hidden"
          >
            <div class="flex justify-between mb-2 text-sm">
              <span class="text-gray-600">Mã:</span>
              <span class="font-semibold text-gray-900" id="coupon-code-display"
                >--</span
              >
            </div>
            <div class="flex justify-between mb-2 text-sm">
              <span class="text-gray-600">Giảm:</span>
              <span
                class="font-semibold text-green-600"
                id="discount-description"
                >--</span
              >
            </div>
            <div class="flex justify-between mb-2 text-sm">
              <span class="text-gray-600">Tiền giảm:</span>
              <span class="font-semibold text-green-600" id="discount-amount"
                >0₫</span
              >
            </div>
            <div
              class="flex justify-between border-t border-green-200 pt-2 text-lg font-bold"
            >
              <span>Thành tiền:</span>
              <span class="text-green-600" id="final-total"
                >{{ formatPrice tour.price }}₫</span
              >
            </div>
          </div>
        </div>

        <!-- Next Button -->
        <button
          type="button"
          id="next-step-1"
          class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 font-semibold text-lg"
        >
          Tiếp tục đến thanh toán
        </button>
      </div>

      <!-- Step 2: Thanh toán -->
      <div id="step-2-content" class="space-y-6 hidden">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">Thanh toán</h2>

        <!-- Order Summary -->
        <div class="bg-gray-50 border border-gray-200 rounded-lg p-6 space-y-4">
          <h3 class="font-semibold text-gray-900 text-lg">Tóm tắt đơn hàng</h3>

          <div class="space-y-2 text-sm border-b border-gray-300 pb-4">
            <div class="flex justify-between">
              <span class="text-gray-600">Tour:</span>
              <span class="font-medium text-gray-900">{{ tour.name }}</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600">Số khách:</span>
              <span class="font-medium text-gray-900" id="summary-guests"
                >1 người</span
              >
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600">Ngày khởi hành:</span>
              <span class="font-medium text-gray-900" id="summary-date"
                >--</span
              >
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600">Giá cơ bản:</span>
              <span class="font-medium text-gray-900" id="summary-subtotal"
                >{{ formatPrice tour.price }}₫</span
              >
            </div>
          </div>

          <div
            id="summary-discount"
            class="hidden space-y-2 text-sm border-b border-gray-300 pb-4"
          >
            <div class="flex justify-between">
              <span class="text-gray-600">Mã giảm giá:</span>
              <span class="font-medium text-gray-900" id="summary-coupon-code"
                >--</span
              >
            </div>
            <div class="flex justify-between text-green-600">
              <span>Tiền giảm:</span>
              <span class="font-semibold" id="summary-discount">0₫</span>
            </div>
          </div>

          <div
            class="flex justify-between text-lg font-bold bg-white border border-gray-300 rounded-lg p-4"
          >
            <span>Tổng thanh toán:</span>
            <span class="text-blue-600" id="summary-total"
              >{{ formatPrice tour.price }}₫</span
            >
          </div>
        </div>

        <!-- Payment Method -->
        <div class="border border-gray-300 rounded-lg p-6">
          <h3 class="font-semibold text-gray-900 mb-4">
            Phương thức thanh toán
          </h3>
          <div class="space-y-3">
            <label
              class="flex items-center p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"
            >
              <input
                type="radio"
                name="payment-method"
                value="momo"
                class="w-4 h-4 text-blue-600"
                checked
              />
              <span class="ml-3 font-medium text-gray-900">Ví Momo</span>
              <span class="ml-auto text-sm text-gray-500"
                >Sẽ được hướng dẫn chuyển khoản</span
              >
            </label>
            <label
              class="flex items-center p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"
            >
              <input
                type="radio"
                name="payment-method"
                value="bank"
                class="w-4 h-4 text-blue-600"
              />
              <span class="ml-3 font-medium text-gray-900"
                >Chuyển khoản ngân hàng</span
              >
            </label>
            <label
              class="flex items-center p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"
            >
              <input
                type="radio"
                name="payment-method"
                value="cod"
                class="w-4 h-4 text-blue-600"
              />
              <span class="ml-3 font-medium text-gray-900"
                >Thanh toán khi tour</span
              >
            </label>
          </div>
        </div>

        <!-- Payment Note -->
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
          <p class="text-sm text-blue-900">
            <strong>Lưu ý:</strong> Hệ thống thanh toán đang ở chế độ demo. Nhấn
            "Xác nhận thanh toán" để hoàn tất đơn hàng.
          </p>
        </div>

        <!-- Action Buttons -->
        <div class="flex space-x-4">
          <button
            type="button"
            id="back-to-step-1"
            class="flex-1 bg-gray-300 text-gray-800 py-3 rounded-lg hover:bg-gray-400 font-semibold"
          >
            ← Quay lại
          </button>
          <button
            type="button"
            id="confirm-payment"
            class="flex-1 bg-green-600 text-white py-3 rounded-lg hover:bg-green-700 font-semibold text-lg"
          >
            Xác nhận thanh toán
          </button>
        </div>
      </div>

      <!-- Step 3: Thành công -->
      <div id="step-3-content" class="hidden text-center space-y-6 py-12">
        <div class="flex justify-center mb-6">
          <div
            class="w-24 h-24 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-5xl"
          >
            ✓
          </div>
        </div>

        <h2 class="text-3xl font-bold text-gray-900">Đặt tour thành công!</h2>

        <div
          class="bg-green-50 border border-green-200 rounded-lg p-6 text-left space-y-3"
        >
          <p class="text-gray-700">
            <strong>Cảm ơn bạn đã đặt tour!</strong> Chúng tôi đã nhận đơn hàng
            của bạn.
          </p>
          <div class="space-y-2 text-sm">
            <div class="flex justify-between">
              <span class="text-gray-600">Mã đơn hàng:</span>
              <span class="font-mono font-bold text-gray-900" id="booking-code"
                >--</span
              >
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600">Tổng tiền:</span>
              <span class="font-bold text-gray-900" id="booking-total">--</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600">Ngày khởi hành:</span>
              <span class="font-bold text-gray-900" id="booking-date">--</span>
            </div>
          </div>
          <p class="text-sm text-gray-600 pt-3 border-t border-green-200">
            Vui lòng kiểm tra email để nhận xác nhận chi tiết. Đội tổ chức tour
            sẽ liên hệ với bạn trong 24 giờ.
          </p>
        </div>

        <!-- Action Buttons -->
        <div class="flex space-x-4">
          <a
            href="/"
            class="flex-1 bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 font-semibold text-center"
          >
            ← Quay lại trang chủ
          </a>
          <a
            href="/my-bookings"
            class="flex-1 bg-gray-600 text-white py-3 rounded-lg hover:bg-gray-700 font-semibold text-center"
          >
            Xem đơn hàng của tôi →
          </a>
        </div>
      </div>
    </div>
  </div>
</div>

<script type="module" src="/client/js/booking.js"></script>
